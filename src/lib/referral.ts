// Central referral/conversion link. Every CTA points here via <CTAButton />.
// The real destination lives in `public/_redirects` — change it there only,
// no rebuild needed.
export const REF_URL = '/info/';
